<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema para toda a aplicação -->
    <style name="themeDemosAndroid" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Personalize seu tema aqui -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
    </style>

    <!-- Tema específico para atividades que precisam de uma barra de ação -->
    <style name="Theme.DemosAndroid.ActionBar" parent="Theme.AppCompat.DayNight">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
    </style>

    <style name="Theme.DemosAndroid" parent="android:Theme.Material.Light.NoActionBar" />
</resources>